# Test Workspace

This is a test workspace for the file-query skill.

## Purpose

Used to verify that the agent can correctly browse and read files.

## Structure

- `data/` — sample data files (CSV, JSON)
- `src/` — source code directory
- `logs/` — log output (normally empty)
